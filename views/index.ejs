<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Todo App</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="table-container">
    <h2 class="todo-title">todo list</h2>
    <div>
      <a class="btn" href="/add-todo">Add Todo</a>
    </div>
    <br>

    <table>
      <tr>
        <th>Title</th>
        <th>Desc</th>
        <th>Created at</th>
        <th>Updated at</th> <!-- Corrected header -->
        <th>Action</th>
      </tr>
      <% for (let i=0; i < todos.length; i++) { %>
        <tr>
          <td>
            <%= todos[i].title %>
          </td>
          <td>
            <%= todos[i].desc %>
          </td>
          <td>
            <%= moment(todos[i].createdAt).format("MMM DD, YYYY, HH:mm:ss") %> <!-- Corrected here -->
          </td>
          <td>
            <%= moment(todos[i].updatedAt).format("MMM DD, YYYY, HH:mm:ss") %> <!-- Corrected here -->
          </td>
          <td>
            <a class="btn" href="/update-todo?id=<%= todos[i]._id %>">Edit</a>
            <a class="btn" href="/delete-todo?id=<%= todos[i]._id %>">Delete</a>
          </td>
        </tr>
        <% } %>
    </table>
  </div>
</body>

</html>